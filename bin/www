#!/usr/bin/env node

var app = require("../app");
var config = require("config");
var port = process.env.PORT || config.port;

app.set("port", port);

var server = app.listen(port, function() {
  app.logger.info("backbone running at http://localhost:" + port);
});